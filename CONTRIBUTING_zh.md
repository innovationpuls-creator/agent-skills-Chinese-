# 为 Agent Skills 做贡献

感谢你的贡献兴趣！本项目是 AI 编程代理的生产级工程技能集合。

## 添加新技能

1. 在 `skills/` 下创建一个以 kebab-case 命名的目录
2. 按照 [docs/skill-anatomy.md](docs/skill-anatomy.md) 中的格式添加 `SKILL.md`
3. 包含带有 `name` 和 `description` 字段的 YAML frontmatter
4. 确保 `description` 简要说明技能的功能（第三人称），然后包含 `Use when` 触发条件

### 技能质量门槛

技能应该是：

- **具体的** — 可操作的步骤，而非模糊的建议
- **可验证的** — 有明确证据要求的退出标准
- **久经考验的** — 基于真实工程工作流，而非理论理想
- **极简的** — 仅包含正确引导代理所需的必要内容

### 结构

每个新技能必须包含：

- 技能目录中的 `SKILL.md`
- 带有有效 `name` 和 `description` 的 YAML frontmatter

新技能通常应遵循标准结构：

- **Overview（概述）** — 这个技能做什么，以及为什么重要
- **When to Use（使用场景）** — 触发条件
- **Process（流程）** — 分步工作流
- **Common Rationalizations（常见自我合理化）** — 代理用来跳过步骤的借口，以及反驳
- **Red Flags（危险信号）** — 警告迹象，表明技能被错误应用
- **Verification（验证）** — 如何确认技能被正确应用

### 不应该做的事

- 不要在技能之间重复内容 —— 改为引用其他技能
- 不要添加模糊建议而非可操作流程的技能
- 除非内容超过 100 行，否则不要创建支撑文件
- 不要把参考资料放在技能目录内 —— 使用 `references/` 目录

## 修改现有技能

- 保持变更聚焦和最小化
- 保留现有结构和语气
- 编辑后确保 YAML frontmatter 仍然有效

## 报告问题

如果你发现以下情况，请提交 issue：

- 技能给出了错误或过时的指导
- 常见工程工作流缺少覆盖
- 技能之间存在不一致

## 许可证

通过贡献，你同意你的贡献将在 MIT 许可证下授权。
